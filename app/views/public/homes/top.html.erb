<div class="w-100 hero_img">
  <div class="position-absolute w-100">
    <%= image_tag "pasta-1854245_1280.jpg", class: 'pasta_img' %>
  </div>
  <div class="position-relative w-100 h-100 d-flex flex-column align-items-center justify-content-center">
    <h2 class="hero_title">Ndeli</h2>
    <p>～～あなたの為のレシピで健やかな毎日を～～</p>
  </div>
</div>

<div class="container mt-5 mb-5">
  <h4>新着レシピ</h4>
  <div class="row mt-3 mb-4">
  <table>
    <tbody>

      <% @recipes.each do |recipe| %>
        <div class="top_recipe">
          <div class="recipe_image">
            <%= link_to recipe_path(recipe.id) do %>
              <%= image_tag recipe.get_image(360,360), class: 'rounded img img-responsive full-width' %>
            <% end %>
          </div>
          <div>
            <h4><%= recipe.dish_name %></h4>
          </div>
          <div>
            <%= recipe.recipe_description %>
          </div>
        </div>
        <% end %>
    </tbody>
  </table>
  </div>

  <%= link_to "すべてのレシピを見る", recipes_path %>
  <%= paginate @recipes %>
</div>